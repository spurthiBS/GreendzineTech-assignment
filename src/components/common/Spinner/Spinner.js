import "./Spinner.css"

function Spinner() {
    return (
        <div className="background-blur">
            <div class="spinner-border spinner-border-lg spinner"></div>
        </div>
     );
}

export default Spinner;